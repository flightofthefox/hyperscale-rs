[package]
name = "hyperscale-effects-bridge"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "The workspace's binding to the VM effect vocabulary: protocol hasher and declared-effects adapter"

[dependencies]
arc-swap.workspace = true
blake3.workspace = true
hyperscale-types.workspace = true
hyperscale-hbor.workspace = true
hyperscale-vm-effects.workspace = true
hyperscale-vm-types.workspace = true
hyperscale-vm-fixtures.workspace = true
# The publish gate. A verdict that is a pure function of the artifact
# belongs beside the vocabulary it judges, so `cargo hyperscale` and
# admission reach one call rather than two implementations.
hyperscale-vm-gate.workspace = true
hyperscale-vm-manifest-builder.workspace = true
hyperscale-vm-stdlib.workspace = true
# The profile validator, without the blessed engine: a publish verdict is
# the same verdict wherever it is reached.
hyperscale-vm-runtime.workspace = true

[dev-dependencies]
hyperscale-types = { workspace = true, features = ["test-utils"] }
wat.workspace = true

[lints]
workspace = true
